/**
 * FICHIER D'EXEMPLE - À SUPPRIMER LORS DE L'IMPLÉMENTATION
 * 
 * Exemple de test Vitest pour FiltersPanel.vue
 * Vous devez créer le vrai fichier FiltersPanel.test.ts
 */

import { describe, it, expect } from 'vitest'
// import { mount } from '@vue/test-utils'
// import FiltersPanel from './FiltersPanel.vue'
// import type { PatientsFilters } from '@/types/patients'

describe('FiltersPanel', () => {
  it('devrait synchroniser v-model:filters avec le parent', () => {
    // TODO: Tester que quand je saisis dans le composant, le parent reçoit les valeurs
    // TODO: Tester que quand le parent change la prop, le panneau se met à jour
  })
})

